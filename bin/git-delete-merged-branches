#!/usr/bin/env bash
# -*- mode: sh; coding: utf-8; tab-width: 4; indent-tabs-mode: nil -*-
set -euo pipefail
IFS=$'\n\t'

git branch --merged | { grep -v -E '^\*|mirror' || true; } | sed "s/^\+//" | sort | xargs -r git branch -d
